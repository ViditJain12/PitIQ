version: "3.9"

services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models
      - ./data:/app/data
    environment:
      - PITIQ_ENV=production

  frontend:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - backend
